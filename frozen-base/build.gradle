group 'org.xblackcat'
version pluginVersion

def generatedDir = file('src/main/gen')

dependencies {
    api project(':jps-plugin')
}

sourceSets {
    main.java.srcDirs += generatedDir
}

idea {
    module {
        generatedSourceDirs += generatedDir
    }
}